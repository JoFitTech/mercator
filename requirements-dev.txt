# Dev- und Test-Abhängigkeiten für Mercator
# Bindet bewusst auch die App-Abhängigkeiten ein, damit E2E-Tests lokal
# gegen eine echte Streamlit-Instanz reproduzierbar laufen.

-r requirements.txt

# E2E Browser-Tests (Playwright)
playwright>=1.44.0
pytest-playwright>=0.5.0

# pytest-Erweiterungen
pytest>=9.0.2
pytest-timeout>=2.3.1

# Statische Prüfung / Hilfstools
python-dotenv>=1.2.2


